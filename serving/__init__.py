"""LLM serving layer over the `llm_inference_engine` package.

Public surface is declared here once the components exist (Phase 1+).
Kept deliberately empty during Phase 0 so that nothing is exported before
it has a correctness gate behind it.
"""

__version__ = "0.0.0"
